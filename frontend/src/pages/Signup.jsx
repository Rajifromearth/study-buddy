import { useState } from 'react'
import { Link, Navigate, useNavigate } from 'react-router-dom'
import AuthLayout from '../components/AuthLayout'
import { useAuth } from '../components/AuthContext'

const API_URL = import.meta.env.VITE_API_URL
export default function Signup() { const [username,setUsername]=useState(''),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[confirm,setConfirm]=useState(''),[error,setError]=useState(''),[loading,setLoading]=useState(false),navigate=useNavigate(),{ logIn }=useAuth(); const submit=async e=>{e.preventDefault();if(!username||!email||!password||!confirm)return setError('Please complete every field.');if(username.trim().length<2)return setError('Username must be at least 2 characters.');if(password!==confirm)return setError('Passwords do not match.');if(password.length<6)return setError('Password must be at least 6 characters.');try{setLoading(true);setError('');const response=await fetch(`${API_URL}/auth/signup`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({username,email,password})});const data=await response.json();if(!response.ok)throw Error(data.detail||'Unable to create your account.');logIn(data.token,data.user);navigate('/dashboard')}catch(err){setError(err.message)}finally{setLoading(false)}};return localStorage.getItem('study_buddy_token') ? <Navigate to="/dashboard" replace /> : <AuthLayout title="Create your account" subtitle="Start building a study rhythm that works for you." footer={<>Already have an account? <Link to="/login">Log in</Link></>}><form className="auth-form" onSubmit={submit}><label>Username<input type="text" value={username} onChange={e=>setUsername(e.target.value)} autoComplete="username" required /></label><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} autoComplete="email" /></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="new-password" /></label><label>Confirm password<input type="password" value={confirm} onChange={e=>setConfirm(e.target.value)} autoComplete="new-password" /></label>{error&&<p className="auth-error" role="alert">{error}</p>}<button className="button button--accent" disabled={loading}>{loading?'Signing up...':'Sign Up'}</button></form></AuthLayout>}